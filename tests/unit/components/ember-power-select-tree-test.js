import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('ember-power-select-tree', 'Unit | Component | ember power select tree', {
  unit: true
});

test('it renders', function(assert) {
  this.subject();
});
